name: first-pipeline

on: 
  push:
       branches: [master]
  workflow_dispatch: 
jobs:
  job-1:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@5
      - run: python app
      
  
